//CSS

const random = document.querySelector(".random");

//random.style.backgroundColor = "blue"; -> every property is added line by line

random.classList.add("title"); //enables the whole class in one turn

